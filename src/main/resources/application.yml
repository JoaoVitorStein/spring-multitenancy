spring:
  jpa:
    database: "postgresql"
    show-sql: true
    hibernate:
      ddl-auto: "none"
      naming:
        physical-strategy: "org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl"
  datasource:
    initialize: false
